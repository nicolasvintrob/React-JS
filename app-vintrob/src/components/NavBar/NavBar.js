const NavBar = () => {
    return (
        <nav>
            <button>Inicio</button>
            <button>Categorias</button>
            <button>Nosotros</button>
        </nav>
    )
}

export default NavBar